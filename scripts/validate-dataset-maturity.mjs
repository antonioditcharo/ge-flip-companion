import{auditDataset,assignDurationSplits}from'../lib/ml/dataset-maturity.mjs';const start=Date.parse('2026-01-01T00:00:00Z'),rows=[];for(let i=0;i<=24*12*22;i++)rows.push({bucket_at:new Date(start+i*300000).toISOString(),item_id:String(i%3),split:i<4000?'train':i<5000?'validation':'test'});const audit=auditDataset(rows,{minTrainDays:14,minValidationDays:3,minTestDays:3}),split=assignDurationSplits(rows,{validationDays:3,testDays:3,embargoMinutes:240});if(audit.availableDays<22||audit.gaps.length)throw Error('Audit duration failed');if(!split.rows.some(r=>r.split==='train')||!split.rows.some(r=>r.split==='validation')||!split.rows.some(r=>r.split==='test'))throw Error('Duration split failed');if(split.dropped.length===0)throw Error('Embargo rows were not removed');console.log('Dataset maturity validation passed.');console.table(Object.entries(audit.bySplit).map(([name,x])=>({name,...x})));console.log(split.boundaries);
