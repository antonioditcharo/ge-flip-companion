import{simulate}from"../lib/ml/simulator.mjs";const start=Date.parse("2026-01-01T00:00:00Z"),rows=[];for(let i=0;i<60;i++)rows.push({item_id:"1",item_name:"Fixture",bucket_at:new Date(start+i*300000).toISOString(),midpoint:String(100+i),momentum_60m:"0.02"});const a=simulate(rows,{startingCash:10000,allocation:.5,maxPositions:1,holdMinutes:60,threshold:.01,costBps:0,slippageBps:0,cooldownMinutes:60,minCompletedTrades:1}),b=simulate(rows,{startingCash:10000,threshold:1}),c=simulate(rows.slice(0,5),{startingCash:10000,holdMinutes:60,threshold:.01,forceLiquidation:true,minCompletedTrades:1});if(!a.trades.length||a.summary.finalEquity<=10000)throw Error("Expected profitable closed trades");if(b.summary.trades!==0)throw Error("Threshold should prevent trades");if(c.summary.openPositions!==0||!c.trades.some(t=>t.reason==='end_of_period'))throw Error("Forced liquidation failed");if(!Array.isArray(a.itemAttribution)||!Number.isFinite(a.summary.totalFees))throw Error("Diagnostics missing");console.log("Enhanced simulator validation passed.");console.table([a.summary,b.summary,c.summary]);
